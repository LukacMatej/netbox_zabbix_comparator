image: docker:latest

stages:
  - docker_build

docker_build_devel:
  stage: docker_build
  script:
    - echo "Using docker ${DOCKER_HOST}"
    - docker build -t netsystemcz/$CI_PROJECT_NAME:test .
  except:
    - tags
