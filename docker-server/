from flask import Flask, request, abort
import subprocess
import argparse
app = Flask(__name__)

@app.route('/webhook',methods=['POST'])
def webhook():
    if request.method == 'POST':
        #subprocess.run(["python",'\\home\\mates\\docker-server\\netbox-zabbix-sync-main\\netbox_zabbix_sync.py'])
        return 'success', 200
    else:
        abort(400)
@app.route('/')
def hello_world():
    return 'Hello'
        
def parser_init():
    # Arguments parsing
    parser = argparse.ArgumentParser(
        description='Turn on/off production.'
    )
    parser.add_argument("-d", "--development", help="Turn on development server",
                        action="store_true")
    parser.add_argument("-i", "--ip", help="Listening on ip address",
                        action="store")
    parser.add_argument("-port", "--port", help="Listening on port",
                        action="store")
    return parser
def main(args):
    if not args.development:
        #production
        from waitress import serve
        serve(app, host=args.ip if args.ip is not None else "0.0.0.0", port=args.port if args.port is not None else "5000")
    else:
        #development
        app.run(debug=True, host="0.0.0.0", port="5000")

    
if __name__ == '__main__':
    parser = parser_init()
    args = parser.parse_args()
    main(args)
    
